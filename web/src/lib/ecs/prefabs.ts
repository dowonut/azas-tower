import { addComponent, addPrefab } from "bitecs";
import { Name, Position } from "./components";

// export const Player = addPrefab(world);
// addComponent(world, Player, [Position, Name]);
// Position[Player] = { x: 0, y: 0 };
// Name[Player] = "Unknown Player";
