/** A simple direction */
export enum Direction {
  Up,
  Down,
  Left,
  Right,
}

/** A cardinal direction */
export enum Heading {
  N,
  NE,
  E,
  SE,
  S,
  SW,
  W,
  NW,
}

/** An entity's animation state */
export enum EntityAnimationState {
  Idle,
  Walking,
  Running,
}
